public class Study24 {

    public static void main(String[] args) {
        
        // Q1. 자판기 프로그램을 만들기 위해서 필요한 기술은 무엇인가요?
        // 1. 조건문 (if문)
        // 2. 입출력 관련 기능
        // 3. 판매 상품에 대한 정보
        // 4. 상품 정보가 연계되는 자료형인 맵과 같은 데이터
        // 5. 조건문
        // 6. 프로그램 언어인 자바
        // 7. 2차원 배열

        // 요구사항
        // 1. 반복 입력 받을 수 있도록 (반복문과 입력(System.in) 필수)
        // 2. 프로그램 시작 시 인사말이 출력되도록
        // 3. 프로그램 종료를 위해 입력으로 "EXIT" 값을 받으면 종료되도록 할 것

        /*
         * 음료는 다섯 종류만...
         * map
         * 
         * 
         * String pay = scanner.next();
         * int balance = scanner.nextint();
         * 
         * if {올바른 음료 선택}
         *      if{현금}
         *          if{투입금>=가격}
         *              sout(종류)
         *              sout(가격)
         *              sout(투입금-가격) 
         *          else{sout("잔액이 부족합니다.")}
         *      else if{카드}
         *          *          if{투입금>=가격}
         *              sout(종류)
         *              sout(가격)
         *              sout(투입금-가격) 
         *          else{sout("잔액이 부족합니다.")}
         *      else{sout("올바른 결제 수단이 아닙니다.")}
         * else{sout("음료를 선택해주세요.")}
         */

    }
    
}

